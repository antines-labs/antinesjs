# @antines/config

Shared TypeScript and tooling configuration for the Antines monorepo.

## Contents

- `tsconfig.base.json` — Base TypeScript config extended by all packages (bundler module resolution, strict mode, verbatim module syntax)
- `turbo.json` generator inputs — glob patterns for task caching

## License

MIT
